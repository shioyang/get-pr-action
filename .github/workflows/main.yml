on:
  - push

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Hello world action step
      id: hello
      uses: shioyang/get-pr-action@v2.0.1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
